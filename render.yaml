services:
  - type: web
    name: no-survivors
    runtime: static
    buildCommand: cd browser && npm install && npx vite build
    staticPublishPath: browser/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
